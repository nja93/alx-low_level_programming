#include <stdio.h>
/**
 * main - Entry point
 * Description: code that computes int of a between 0 and 10
 *
 * Return: Always 0 (success)
 */
int main(void)
{
/* start of code block */
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
/* code that computes int of a between 0 and 10 */
	return (0);
}
