#include "main.h"
#include <string.h>

/**
 * _strlen - Computes the lenght of a character
 * @s: string pointer passed to this function
 * Return: returns the length of the string
 */

int _strlen(char *s)
{
		return (strlen(s));
}
